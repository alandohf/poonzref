#include <stdio.h>
#include <stdlib.h>
#include "stack.h"


int
main(){

     char * pChar = (char*) malloc(STACK_SIZE);
	 NODE * pNode = (NODE*)pChar;
	 
	 STACK * s;
	 
	 
	
	
	return 0;
}
